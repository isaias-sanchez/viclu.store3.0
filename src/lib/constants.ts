export const WHATSAPP_PHONE = import.meta.env.VITE_WHATSAPP_PHONE ?? "573155928724";
export const CURRENCY = "COP";

export const APP_CONFIG = {
    // ⚠️ Auth migrated to Supabase Auth — admin password no longer stored in code.
    // Create an admin user in your Supabase dashboard → Authentication → Users.
    instagramUrl: import.meta.env.VITE_INSTAGRAM_URL ?? 'https://www.instagram.com/viclustore07'
};
